import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.indiedev.wedinv',
  appName: 'WedInv',
  webDir: 'dist/wedinv-web/browser'
};

export default config;
